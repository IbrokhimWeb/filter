export type LoginPostType = {
  email: string;
  password: string;
};

export type HomeFilterType = {
  country: string;
  region: string;
  organ: string;
  file_date: string;
  ordering: string;
};
